# Setup on raspberry and set to launch hardware.js on startup

#Enable the ssh server here
sudo raspi-config

# Get node from nodesource
sudo apt-get update
sudo apt install -y nodejs

# Download the repository
